/**
 * Search-related constants used across the application
 */

/**
 * Debounce delay for search input in milliseconds
 */
export const SEARCH_DEBOUNCE_MS = 300;

/**
 * Cache time-to-live for search query data in milliseconds (5 minutes)
 */
export const QUERY_CACHE_TTL = 5 * 60 * 1000;

/**
 * Suffix used for use case description matches in search results
 */
export const USE_CASE_DESC_SUFFIX = "-desc";
