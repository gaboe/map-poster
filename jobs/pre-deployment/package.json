{
  "name": "@map-poster/pre-deployment",
  "version": "0.1.0",
  "description": "Pre-deployment jobs for database migration and other setup tasks",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "db-migrate": "bun run db-migrate.ts",
    "build": "bun build index.ts --outdir=dist --target=node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@map-poster/common": "workspace:*",
    "@map-poster/db": "workspace:*",
    "drizzle-kit": "catalog:",
    "drizzle-orm": "catalog:",
    "pg": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "typescript": "catalog:"
  }
}
