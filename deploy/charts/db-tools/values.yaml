image:
  repository: blogicapps2-fgbjaff4hhdzcga7.azurecr.io/pg-client
  pullPolicy: IfNotPresent
  tag: "18"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

db:
  host: postgres-postgresql.database
  database: projecttemplate

credentials:
  # Set create to false if secret is already created in the desired namespace
  create: true
  username: projecttemplate
  password:
  # Superadmin credentials for database/user creation
  sa_username: postgres
  sa_password:

backupJob:
  create: false
  schedule: "0 2 * * *"

initdb:
  create: true

podAnnotations: {}

podSecurityContext:
  {}
  # fsGroup: 2000

securityContext:
  {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

resources:
  {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
